import type { ReactNode } from 'react';
export function DataTable<T>({ columns, rows, empty='No records found.' }: { columns:{key:string; header:string; render:(row:T)=>ReactNode}[]; rows:T[]; empty?:string }) { return <div className="overflow-x-auto rounded-xl border border-slate-200 bg-white"><table className="min-w-full"><thead className="table-head"><tr>{columns.map(c=><th key={c.key} className="px-4 py-3">{c.header}</th>)}</tr></thead><tbody>{rows.length?rows.map((r,i)=><tr key={i}>{columns.map(c=><td className="table-cell" key={c.key}>{c.render(r)}</td>)}</tr>):<tr><td className="px-4 py-10 text-center text-sm text-slate-500" colSpan={columns.length}>{empty}</td></tr>}</tbody></table></div>; }
