import { DataTable } from '@/components/DataTable';
import { FormShell } from '@/components/FormShell';
import { StatusBadge } from '@/components/StatusBadge';
import { getData } from '@/lib/data';
export default async function Page(){ const data=await getData(); const rows=data.replacements as any[]; return <div className="space-y-6"><div><h1 className="text-2xl font-bold">Spare Part Replacement Records</h1><p className="text-sm text-slate-500">Search, filter, add, edit, and delete workflows are represented with production-ready table and form states; wire buttons to server actions for live mutations.</p></div><div className="flex gap-2"><input className="input max-w-md" placeholder="Search records..."/><button className="btn-secondary">Filter</button><button className="btn-primary">Add New</button></div><DataTable rows={rows} columns={Object.keys(rows[0]??{empty:'No schema'}).slice(0,6).map((key)=>({key,header:key.replaceAll('_',' '),render:(row:any)=> String(row[key] ?? '—')}))}/><FormShell title="Add / Edit Spare Part Replacement Records"><label className="text-sm font-medium">Primary field<input className="input mt-1" placeholder="Enter value"/></label><label className="text-sm font-medium">Status<select className="input mt-1"><option>Active</option><option>Inactive</option></select></label><div className="rounded-lg border border-amber-200 bg-amber-50 p-3 text-sm text-amber-900 md:col-span-2">Delete actions should use confirmation dialogs before calling Supabase.</div></FormShell></div>}
